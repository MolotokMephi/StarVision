"""
ai_assistant.py — StarAI: ИИ-ассистент, способный отвечать на вопросы
и генерировать команды управления интерфейсом.
"""

import json
import os
from typing import Dict, Any, List, Optional

import httpx
import logging

# Anthropic API (через HTTP, без SDK — для простоты)
ANTHROPIC_API_URL = "https://api.anthropic.com/v1/messages"
ANTHROPIC_API_KEY = os.getenv("ANTHROPIC_API_KEY", "")
MODEL = "claude-sonnet-4-20250514"

SYSTEM_PROMPT = """Ты — StarAI, интеллектуальный ассистент проекта StarGrid.
Проект StarGrid — цифровой двойник группировки российских кубсатов и малых космических аппаратов.

Твои возможности:
1. Отвечать на вопросы о спутниках, орбитальной механике, космических программах России и мира.
2. Управлять интерфейсом визуализации через специальные команды.
3. Объяснять физику орбитального движения, законы Кеплера, SGP4-модель.
4. Сравнивать группировки (Starlink, OneWeb, Сфера и др.).
5. Рассказывать о межспутниковых связях, их принципах и ограничениях.

ФОРМАТ ОТВЕТА — строго JSON:
{
  "message": "Текст ответа пользователю (на русском)",
  "actions": [
    // Необязательно. Массив команд для интерфейса:
    // {"type": "focus_satellite", "norad_id": 56200}
    // {"type": "set_time_speed", "speed": 10}
    // {"type": "toggle_orbits", "visible": true}
    // {"type": "toggle_links", "visible": true}
    // {"type": "highlight_constellation", "name": "Сфера"}
    // {"type": "set_satellite_count", "count": 10}
    // {"type": "set_comm_range", "range_km": 800}
    // {"type": "set_orbit_altitude", "altitude_km": 600}
    // {"type": "reset_view"}
  ]
}

Российские спутники в системе (NORAD ID):
- Группировка «Сфера»: Скиф-Д (56200), Марафон-IoT-1 (56201), Марафон-IoT-2 (56202)
- Образовательные: СириусСат-1 (44394), СириусСат-2 (44395), УмКА-1 (47951)
- МФТИ: Декарт (49260)
- Группировка «Гонец»: Гонец-М №21 (40553), Гонец-М №22 (40554), Гонец-М №23 (40555)
- ДЗЗ: Зоркий-2М (48850), Беркут-С (55120)
- Научные: Аист-2Т (55121)
- МГТУ им. Баумана: УмКА-1 (47951)

Всегда отвечай на русском языке. Будь дружелюбным, информативным и увлечённым космосом.
Если пользователь просит показать спутник — используй action focus_satellite с правильным norad_id.
Если просит ускорить/замедлить — set_time_speed.
Если спрашивает про количество — можешь использовать set_satellite_count.
Если спрашивает про связи — toggle_links и/или set_comm_range.
Если спрашивает про орбиту — toggle_orbits и/или set_orbit_altitude.
"""


async def ask_starai(
    user_message: str,
    conversation_history: List[Dict[str, str]] = None
) -> Dict[str, Any]:
    """
    Отправить сообщение StarAI и получить ответ + команды.
    """
    if not ANTHROPIC_API_KEY:
        # Фоллбэк без API-ключа — базовые ответы
        return _fallback_response(user_message)

    messages = []
    if conversation_history:
        messages.extend(conversation_history)
    messages.append({"role": "user", "content": user_message})

    try:
        async with httpx.AsyncClient(timeout=30.0) as client:
            response = await client.post(
                ANTHROPIC_API_URL,
                headers={
                    "x-api-key": ANTHROPIC_API_KEY,
                    "anthropic-version": "2023-06-01",
                    "content-type": "application/json",
                },
                json={
                    "model": MODEL,
                    "max_tokens": 1024,
                    "system": SYSTEM_PROMPT,
                    "messages": messages,
                },
            )
            response.raise_for_status()
            data = response.json()

        # Извлекаем текст ответа
        content = data.get("content", [])
        text = ""
        for block in content:
            if block.get("type") == "text":
                text += block.get("text", "")

        # Парсим JSON из ответа
        try:
            parsed = json.loads(text)
            return {
                "message": parsed.get("message", text),
                "actions": parsed.get("actions", []),
            }
        except json.JSONDecodeError:
            return {"message": text, "actions": []}

    except Exception:
        # Логируем подробности ошибки на сервере, не раскрывая их пользователю
        logging.exception("Ошибка соединения с StarAI")
        return {
            "message": "Ошибка соединения с StarAI. Работаю в офлайн-режиме.",
            "actions": [],
        }


def _fallback_response(user_message: str) -> Dict[str, Any]:
    """Офлайн-ответы без API-ключа — расширенный набор команд."""
    msg_lower = user_message.lower().strip()

    import re

    # ── Приветствия ──────────────────────────────────────
    if any(w in msg_lower for w in ["привет", "здравствуй", "хай", "hello", "добрый", "здрасте"]):
        return {
            "message": "Привет! ✦ Я StarAI — интеллектуальный ассистент StarGrid. "
                       "Могу рассказать о спутниках, управлять визуализацией, "
                       "объяснить орбитальную механику и многое другое. Спрашивай!",
            "actions": [],
        }

    # ── Программа Сфера ──────────────────────────────────
    if any(w in msg_lower for w in ["сфера", "скиф", "марафон"]):
        if "марафон" in msg_lower:
            return {
                "message": "«Марафон-IoT» — подсистема программы «Сфера» для Интернета вещей (IoT/M2M). "
                           "Спутники формата 6U обеспечивают передачу данных от датчиков, "
                           "счётчиков и промышленных устройств. В нашей модели два аппарата: "
                           "Марафон-IoT-1 и Марафон-IoT-2. Показываю первый.",
                "actions": [{"type": "focus_satellite", "norad_id": 56201}],
            }
        return {
            "message": "Программа «Сфера» — амбициозный российский проект многофункциональной "
                       "спутниковой группировки. Включает подсистемы:\n"
                       "• «Скиф» — широкополосный интернет (аналог Starlink)\n"
                       "• «Марафон» — IoT/M2M связь\n"
                       "• «Экспресс-РВ» — ретрансляция\n"
                       "Скиф-Д — демонстрационный аппарат, запущенный в 2022 году на орбиту ~550 км. "
                       "Показываю группировку «Сфера».",
            "actions": [
                {"type": "highlight_constellation", "name": "Сфера"},
                {"type": "focus_satellite", "norad_id": 56200},
            ],
        }

    # ── Гонец ────────────────────────────────────────────
    if "гонец" in msg_lower:
        return {
            "message": "«Гонец-М» — российская низкоорбитальная система персональной "
                       "спутниковой связи. Обеспечивает передачу коротких сообщений "
                       "и данных в любой точке мира, включая Арктику. "
                       "Орбита ~82.5°, что обеспечивает полярное покрытие. "
                       "В нашей модели 3 аппарата: №21, №22, №23. Показываю группировку.",
            "actions": [{"type": "highlight_constellation", "name": "Гонец"}],
        }

    # ── Образовательные / Сириус ─────────────────────────
    if any(w in msg_lower for w in ["сириус", "образовани", "школьник", "наноспутник"]):
        return {
            "message": "СириусСат — два наноспутника формата 1U (10×10×10 см, ~1.5 кг), "
                       "созданные школьниками центра «Сириус» совместно с РКК «Энергия». "
                       "Запущены в 2018 году с МКС для мониторинга радиационной обстановки "
                       "и изучения космических лучей. Показываю СириусСат-1.",
            "actions": [
                {"type": "highlight_constellation", "name": "Образовательные"},
                {"type": "focus_satellite", "norad_id": 44394},
            ],
        }

    # ── МФТИ / Декарт ────────────────────────────────────
    if any(w in msg_lower for w in ["мфти", "декарт", "физтех"]):
        return {
            "message": "«Декарт» — спутник МФТИ формата 12U (масса ~50 кг). "
                       "Предназначен для научных экспериментов и дистанционного "
                       "зондирования Земли. Запущен в 2021 году. Показываю.",
            "actions": [{"type": "focus_satellite", "norad_id": 49260}],
        }

    # ── Баумана / УмКА ───────────────────────────────────
    if any(w in msg_lower for w in ["бауман", "умка", "мгту"]):
        return {
            "message": "УмКА-1 — кубсат формата 3U (~3 кг), разработанный "
                       "студентами МГТУ им. Баумана. Технологический демонстратор, "
                       "запущенный в 2021 году. Показываю.",
            "actions": [{"type": "focus_satellite", "norad_id": 47951}],
        }

    # ── ДЗЗ / Зоркий / Беркут ───────────────────────────
    if any(w in msg_lower for w in ["зоркий", "дзз", "зондирован", "съёмк", "беркут", "спутникс"]):
        if "беркут" in msg_lower:
            return {
                "message": "«Беркут-С» — спутник сверхвысокого разрешения для картографии "
                           "и мониторинга. Масса ~200 кг. Запущен в 2023 году.",
                "actions": [{"type": "focus_satellite", "norad_id": 55120}],
            }
        return {
            "message": "Спутники ДЗЗ (дистанционного зондирования Земли) в нашей модели:\n"
                       "• Зоркий-2М (Спутникс) — коммерческий спутник ДЗЗ, ~130 кг\n"
                       "• Беркут-С — высокодетальная съёмка, ~200 кг\n"
                       "Показываю группу ДЗЗ.",
            "actions": [{"type": "highlight_constellation", "name": "ДЗЗ"}],
        }

    # ── Аист / Научные ───────────────────────────────────
    if any(w in msg_lower for w in ["аист", "научн", "прогресс", "самар"]):
        return {
            "message": "«Аист-2Т» — малый космический аппарат (~530 кг), "
                       "разработанный РКЦ «Прогресс» совместно с СГАУ (Самара). "
                       "Предназначен для научных экспериментов и ДЗЗ. Показываю.",
            "actions": [{"type": "focus_satellite", "norad_id": 55121}],
        }

    # ── Управление скоростью ─────────────────────────────
    if any(w in msg_lower for w in ["ускор", "быстр"]):
        # Попытка извлечь число
        nums = re.findall(r'\d+', msg_lower)
        speed = int(nums[0]) if nums else 50
        speed = min(200, max(1, speed))
        return {
            "message": f"Ускоряю симуляцию в {speed}× раз! Спутники будут двигаться быстрее.",
            "actions": [{"type": "set_time_speed", "speed": speed}],
        }

    if any(w in msg_lower for w in ["замедл", "стоп", "пауз", "остано", "медленн", "реальн"]):
        return {
            "message": "Возвращаю реальное время (1×).",
            "actions": [{"type": "set_time_speed", "speed": 1}],
        }

    if any(w in msg_lower for w in ["врем", "скорость"]) and not any(w in msg_lower for w in ["сколько", "какой", "какая"]):
        nums = re.findall(r'\d+', msg_lower)
        if nums:
            speed = min(200, max(1, int(nums[0])))
            return {
                "message": f"Устанавливаю скорость симуляции: {speed}×",
                "actions": [{"type": "set_time_speed", "speed": speed}],
            }

    # ── Орбиты ───────────────────────────────────────────
    if any(w in msg_lower for w in ["орбит", "трек", "траектор"]):
        if any(w in msg_lower for w in ["скры", "убери", "выключ", "спряч"]):
            return {
                "message": "Скрываю орбитальные треки.",
                "actions": [{"type": "toggle_orbits", "visible": False}],
            }
        return {
            "message": "Включаю отображение орбитальных треков. "
                       "Каждая линия показывает путь спутника за один виток.",
            "actions": [{"type": "toggle_orbits", "visible": True}],
        }

    # ── Линии связи ──────────────────────────────────────
    if any(w in msg_lower for w in ["связ", "линии", "мсс", "isl"]):
        if any(w in msg_lower for w in ["скры", "убери", "выключ", "спряч"]):
            return {
                "message": "Скрываю линии межспутниковой связи.",
                "actions": [{"type": "toggle_links", "visible": False}],
            }
        if any(w in msg_lower for w in ["покаж", "включ", "отобраз"]):
            return {
                "message": "Включаю линии межспутниковой связи. Зелёные линии — активные соединения "
                           "в пределах дальности связи. Красные — спутники почти в зоне видимости.",
                "actions": [{"type": "toggle_links", "visible": True}],
            }
        # Информационный ответ про связи
        return {
            "message": "Межспутниковая связь (ISL) — это передача данных между спутниками "
                       "без ретрансляции через наземные станции. В StarGrid мы моделируем ISL "
                       "с учётом:\n"
                       "• Расстояния между спутниками (порог дальности связи)\n"
                       "• Прямой видимости (линия связи не должна пересекать Землю)\n"
                       "Зелёные линии — активные связи, красные — потенциальные.",
            "actions": [{"type": "toggle_links", "visible": True}],
        }

    # ── Количество спутников ─────────────────────────────
    if any(w in msg_lower for w in ["количеств", "сколько спут", "число спут", "добав", "установи"]):
        nums = re.findall(r'\d+', msg_lower)
        if nums:
            count = min(15, max(3, int(nums[0])))
            return {
                "message": f"Устанавливаю {count} спутников в группировке.",
                "actions": [{"type": "set_satellite_count", "count": count}],
            }
        return {
            "message": "Сейчас в модели отображается до 14 реальных российских спутников. "
                       "Ты можешь изменить количество от 3 до 15 через ползунок или сказать мне, "
                       "например: «Установи 10 спутников».",
            "actions": [],
        }

    # ── Дальность связи ──────────────────────────────────
    if any(w in msg_lower for w in ["дальност", "радиус связ", "зона связ"]):
        nums = re.findall(r'\d+', msg_lower)
        if nums:
            rng = min(2000, max(50, int(nums[0])))
            return {
                "message": f"Устанавливаю дальность связи: {rng} км. "
                           "Линии связи будут отображаться между спутниками, "
                           "расстояние между которыми не превышает этот порог.",
                "actions": [{"type": "set_comm_range", "range_km": rng}],
            }
        return {
            "message": "Дальность связи определяет максимальное расстояние между спутниками "
                       "для установления соединения. Типичные значения: 500–1500 км для LEO.",
            "actions": [],
        }

    # ── Высота орбиты ────────────────────────────────────
    if any(w in msg_lower for w in ["высот", "altitude"]):
        nums = re.findall(r'\d+', msg_lower)
        if nums:
            alt = min(2000, max(0, int(nums[0])))
            if alt == 0:
                return {
                    "message": "Переключаюсь на реальные TLE-орбиты спутников.",
                    "actions": [{"type": "set_orbit_altitude", "altitude_km": 0}],
                }
            return {
                "message": f"Устанавливаю высоту виртуальной круговой орбиты: {alt} км. "
                           "Все спутники будут равномерно распределены на этой высоте.",
                "actions": [{"type": "set_orbit_altitude", "altitude_km": alt}],
            }

    # ── Показать спутник по имени ────────────────────────
    if any(w in msg_lower for w in ["покаж", "найди", "где ", "фокус"]):
        sat_map = {
            "скиф": 56200, "марафон-1": 56201, "марафон-2": 56202,
            "сириус-1": 44394, "сириуссат-1": 44394, "сириус-2": 44395,
            "декарт": 49260, "умка": 47951,
            "гонец-21": 40553, "гонец-22": 40554, "гонец-23": 40555,
            "зоркий": 48850, "беркут": 55120, "аист": 55121,
        }
        for name, nid in sat_map.items():
            if name in msg_lower:
                return {
                    "message": f"Навожу камеру на спутник.",
                    "actions": [{"type": "focus_satellite", "norad_id": nid}],
                }

    # ── Сброс ────────────────────────────────────────────
    if any(w in msg_lower for w in ["сброс", "reset", "начал", "по умолч", "верн"]):
        return {
            "message": "Сбрасываю все параметры к начальным значениям.",
            "actions": [{"type": "reset_view"}],
        }

    # ── Кеплер / орбитальная механика ────────────────────
    if any(w in msg_lower for w in ["кеплер", "механик", "физик", "гравитац", "закон"]):
        return {
            "message": "Орбитальная механика основана на законах Кеплера:\n"
                       "1. Орбиты — эллипсы с центром масс в фокусе\n"
                       "2. Радиус-вектор заметает равные площади за равное время\n"
                       "3. T² ∝ a³ (период² пропорционален полуоси³)\n\n"
                       "В StarGrid мы используем модель SGP4 для точного расчёта "
                       "позиций спутников с учётом атмосферного торможения, "
                       "гравитационных гармоник J2 и других возмущений.",
            "actions": [],
        }

    # ── Starlink / OneWeb / сравнение ────────────────────
    if any(w in msg_lower for w in ["starlink", "oneweb", "сравн", "илон", "маск"]):
        return {
            "message": "Сравнение крупнейших группировок:\n"
                       "• Starlink (SpaceX): ~5000+ спутников, 550 км, ШПД\n"
                       "• OneWeb: ~600 спутников, 1200 км, ШПД\n"
                       "• Сфера (Россия): планируется 600+ КА, связь + IoT + ДЗЗ\n"
                       "• Гонец-М: ~12 спутников, 1500 км, персональная связь\n\n"
                       "Российские группировки отличаются акцентом на полярное покрытие "
                       "и комплексность (связь + IoT + наблюдение).",
            "actions": [],
        }

    # ── TLE / SGP4 ───────────────────────────────────────
    if any(w in msg_lower for w in ["tle", "sgp4", "двухстрочн", "элемент"]):
        return {
            "message": "TLE (Two-Line Elements) — двухстрочные элементы орбиты. "
                       "Это стандартный формат описания орбиты, включающий:\n"
                       "• Наклонение, RAAN, эксцентриситет\n"
                       "• Аргумент перигея, средняя аномалия\n"
                       "• Среднее движение (обороты/сутки)\n\n"
                       "SGP4 — аналитическая модель распространения, которая учитывает "
                       "возмущения J2, атмосферное торможение и другие эффекты. "
                       "Мы используем её для расчёта позиций в реальном времени.",
            "actions": [],
        }

    # ── Кубсат / формфактор ──────────────────────────────
    if any(w in msg_lower for w in ["кубсат", "cubesat", "формфактор", "форм-фактор", "размер"]):
        return {
            "message": "CubeSat — стандарт малых спутников:\n"
                       "• 1U: 10×10×10 см, ~1.3 кг (СириусСат)\n"
                       "• 3U: 10×10×30 см, ~4 кг (УмКА-1)\n"
                       "• 6U: 10×20×30 см, ~12 кг (Марафон-IoT)\n"
                       "• 12U: 20×20×30 см, ~24 кг (Декарт)\n\n"
                       "Стандартизация позволяет удешевить запуск и использовать "
                       "унифицированные пусковые контейнеры.",
            "actions": [],
        }

    # ── Помощь ───────────────────────────────────────────
    if any(w in msg_lower for w in ["помощ", "help", "команд", "что ты умеешь", "что можешь", "возможност"]):
        return {
            "message": "Вот что я умею:\n"
                       "✦ Рассказать о спутниках: «Расскажи про Сферу», «Что такое Гонец?»\n"
                       "✦ Показать спутник: «Покажи Скиф-Д», «Где Декарт?»\n"
                       "✦ Управлять скоростью: «Ускорь в 50 раз», «Замедли»\n"
                       "✦ Управлять связями: «Покажи связи», «Установи дальность 1000 км»\n"
                       "✦ Менять орбиты: «Высота 600 км», «Установи 10 спутников»\n"
                       "✦ Объяснять механику: «Что такое SGP4?», «Законы Кеплера»\n"
                       "✦ Сравнивать: «Сравни Starlink и Сферу»\n"
                       "✦ Сбросить всё: «Сброс»",
            "actions": [],
        }

    # ── Космос / общие вопросы ───────────────────────────
    if any(w in msg_lower for w in ["космос", "мкс", "ракет", "запуск"]):
        return {
            "message": "Космические запуски малых спутников обычно осуществляются как попутная "
                       "нагрузка на ракетах-носителях «Союз-2», Falcon 9 или специализированных "
                       "носителях для кубсатов. С МКС спутники выводятся через шлюзовую камеру "
                       "модуля Kibo (JAXA). Россия также развивает платформу «Спутникс» "
                       "для серийного производства малых КА.",
            "actions": [],
        }

    # ── Благодарность ────────────────────────────────────
    if any(w in msg_lower for w in ["спасибо", "благодар", "круто", "класс", "отлично"]):
        return {
            "message": "Рад помочь! ✦ Если есть ещё вопросы о спутниках или визуализации — "
                       "спрашивай. Я всегда на связи!",
            "actions": [],
        }

    # ── Общий ответ ──────────────────────────────────────
    return {
        "message": "Я StarAI — интеллектуальный ассистент StarGrid. Вот что я могу:\n"
                   "✦ Рассказать о любом спутнике в группировке\n"
                   "✦ Показать спутник на визуализации\n"
                   "✦ Управлять скоростью, орбитами и связями\n"
                   "✦ Объяснить орбитальную механику и TLE\n\n"
                   "Попробуй: «Расскажи про Сферу», «Покажи Гонец», "
                   "«Ускорь в 50 раз» или «Что такое кубсат?»",
        "actions": [],
    }
